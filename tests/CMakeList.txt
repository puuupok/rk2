cmake_minimum_required(VERSION 3.10)
project(bridge_tests)

set(CMAKE_CXX_STANDARD 11)

# Включите эту строку, если используете Catch2 как подмодуль Git
# add_subdirectory(path/to/catch2)


enable_testing()

add_executable(bridge_tests
    bridge.cpp
    tests.cpp
)

target_include_directories(bridge_tests
    PRIVATE
    ${CATCH_INCLUDE_DIR}
)

add_test(
    NAME bridge_tests
    COMMAND bridge_tests
)

